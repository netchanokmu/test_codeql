name: "CodeQL scan dotnet"

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [ 'csharp' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Download and install CodeQL
        run: |
          curl -sSL https://github.com/github/codeql-cli-binaries/releases/download/v2.15.2/codeql-linux64.zip -o codeql-linux64.zip
          unzip -qq codeql-linux64.zip -d /usr/local/bin
          chmod +x /usr/local/bin/codeql

      - name: Initialize CodeQL for .NET
        uses: github/codeql-action/init@v2
        with:
          languages: ${{ matrix.language }}

      - name: Setup dotnet
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.0.x'
          
      - name: Extract and upload compilation database
        run: /usr/local/bin/codeql database prepare --language=csharp --output=/dotnet-code-folder/db

      - name: Analyze
        run: /usr/local/bin/codeql database analyze /dotnet-code-folder/db ${{ matrix.language }}-security-extended.qls
